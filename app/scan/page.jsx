import ScanSimulationClient from "../../components/ScanSimulationClient";

export const metadata = {
  title: "Free Credit Scan",
  description: "Simulate a Credit Vivo report upload, review, updates, and follow-up workflow.",
};

export default function ScanPage() {
  return <ScanSimulationClient />;
}
