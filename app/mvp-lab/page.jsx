import MvpLabClient from "../../components/MvpLabClient";

export const metadata = {
  title: "MVP Lab",
  description: "Internal Credit Vivo MVP test lab for unfinished vendor, compliance, and backend features.",
};

export default function MvpLabPage() {
  return <MvpLabClient />;
}
